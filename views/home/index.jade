extends ./main.jade

block content
  include ./filter.jade
  #map(class="map form home-map")
  #content
    div(class="location-form")
      h1 Food for Health
      form(id="find-location")
        div(id="location-search-input-container")
          input(type="text" ref="location" id="location-input" class="search"  placeholder="Enter a Location")
          button(id="geolocate-button" class="search-input-button-cap icon-location")
        button(id="location-submit" type="submit" class="submit") Search

  #store-detail

